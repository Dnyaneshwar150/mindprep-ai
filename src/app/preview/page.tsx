"use client";
import { Grid } from "@mui/material";

import PreviewContainer from "@/Components/PreviewComponents/PreviewContainer";

export default function Home() {
  return (
    <Grid>
      <PreviewContainer />
    </Grid>
  );
}
